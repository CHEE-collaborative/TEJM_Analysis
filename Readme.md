### Documentation

This project involves code works and necessary datasets for our analysis. To run this project, open` _targets.R` and try

```R
library(targets)
library(tarchetypes)
tar_make()
```

The whole process takes 10 to 20 hours and requires at least 30 GB of memory.

